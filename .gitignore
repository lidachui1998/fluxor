node_modules
dist
frontend/dist
backend/dist
build
fluxor
upx-ucl
.npm-*
.gomod*
.gopath*
.gocache*
geo*
mihomo
.tmp-*

# VitePress（docs/）的开发缓存：跑过 docs 的 dev/build 后才会出现，
# 与后端的 .gocache* 同理，属于本地产物而非源码
docs/.vitepress/cache
